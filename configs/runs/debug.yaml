# @package _global_
# Run preset: quick debug-friendly settings
# Keep this for local development overrides.
# Intended for composition as an additional override, e.g. `+runs=debug`.

defaults:
  - _self_

seed: 123
